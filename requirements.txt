# Phase 1-2 (ffx_core, cli.py) use only the Python standard library.
# Phase 3 (ffx_gui) uses PySide2 (Qt5), NOT PySide6 — PySide6/Qt6 dropped
# Windows 7 support entirely (regardless of Python version), which is why
# the app failed to launch with "DLL load failed while importing
# QtWidgets" on Win7. PySide2 is the last Qt release with real Win7
# support.
PySide2>=5.15
